/* dos.h — empty stub for ESP32 build.
 * The Apogee audiolib includes <dos.h> for DOS-specific port I/O.
 * All DOS-specific code in multivoc.c is inside #ifdef PLAT_DOS guards
 * so none of its content is actually needed. */
#pragma once
